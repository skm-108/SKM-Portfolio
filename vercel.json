{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "plugins": ["vercel/vercel-plugin"],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
